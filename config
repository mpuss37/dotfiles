# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

#ukuran border window
default_border pixel 1
default_floating_border pixel 1

#default model workspace layout
workspace_layout tabbed

set $mod Mod4
set $alt Mod1

#autotiling with xdotool
#bindsym $mod+Shift+h exec --no-startup-id xdotool key --clearmodifiers $mod+space

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:monospace 8
font pango:JetBrainsMono ExtraBold 9

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork --no-unlock-indicator --image=/home/mpuss/Pictures/walp/shinobi-un-1336x768.jpg

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
#exec --no-startup-id nm-applet
bindsym $mod+Shift+h exec --no-startup-id /home/mpuss/kodingan/skrip/wifi.sh
bindsym $mod+Shift+d exec --no-startup-id /home/mpuss/kodingan/skrip/dipais-wifi.sh


# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
exec --no-startup-id $refresh_i3status
#bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10%&& $refresh_i3status
#bindsym Ctrl+p exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
#bindsym Ctrl+l exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && pkill -SIGRTMIN+1 i3blocks && $refresh_i3status
bindsym Ctrl+l exec --no-startup-id /home/mpuss/kodingan/skrip/audioturun.sh && $refresh_i3status
bindsym $alt+l exec --no-startup-id /home/mpuss/kodingan/skrip/audioturun.sh && pkill -SIGRTMIN+1 i3blocks && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

#for limiting up volume audio master
bindsym Ctrl+p exec --no-startup-id /home/mpuss/kodingan/skrip/audio.sh && $refresh_i3status
#bindsym $alt+p exec --no-startup-id /home/mpuss/kodingan/skrip/audio.sh && pkill -SIGRTMIN+1 i3blocks && $refresh_i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id /home/mpuss/kodingan/skrip/audio.sh

#Touchpad
#exec --no-startup-id xinput set-prop "9" "libinput Tapping Enabled,libinput Natural Scrolling Enabled" 1
bindsym XF86TouchpadToggle exec --no-startup-id /home/mpuss/kodingan/skrip/touchpadturu.sh

# Use pactl to adjust volume in Brightness.
bindsym XF86MonBrightnessUp exec --no-startup-id brightnessctl set +1%
bindsym Ctrl+o exec --no-startup-id brightnessctl set +1%
bindsym XF86MonBrightnessDown exec --no-startup-id brightnessctl set 1%-
bindsym Ctrl+i exec --no-startup-id brightnessctl set 1%-

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
#bindsym $mod+Shift+Return exec --no-startup-id /home/mpuss/kodingan/skrip/terminal-ngawang.sh
#bindsym $mod+Shift+Return exec --no-startup-id alacritty
bindsym $mod+Return exec --no-startup-id kitty

# start a terminal auto floating mode
#for_window [class="^.*terminal.*$"] floating enable
for_window [title="Event Tester"] floating enable
for_window [class="qpaeq"] floating enable

#minim ukuran mode floating untuk semua window
#floating_minimum_size 80 x 24
#for_window [title="/home/mpuss"] floating enable

# kill focused window
#bindsym $mod+Shift+q kill
bindsym $mod+q kill
bindsym $alt+q kill

# kill focused window but all window
bindsym $mod+Shift+a focus parent,kill

# start dmenu (a program launcher)
#bindsym $mod+d exec --no-startup-id dmenu_run
bindsym $mod+d exec --no-startup-id i3-dmenu-desktop --dmenu="dmenu -i -fn 'JetBrainsMono ExtraBold-9' -nb '#781913' -sb '#E00000' "
# A more modern dmenu replacement is rofi:
# bindcode $mod+40 exec "rofi -modi drun,run -show drun"
# There also is i3-dmenu-desktop which only displays applications shipping a
# .desktop file. It is a wrapper around dmenu, so you need that installed.
# bindcode $mod+40 exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

bindsym $mod+Tab focus right
bindsym $mod+Shift+Tab focus right
bindsym $alt+Tab focus right
bindsym $alt+shift+Tab focus left

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left 10px
bindsym $mod+Shift+k move down 10px
bindsym $mod+Shift+l move up 10px
bindsym $mod+Shift+semicolon move right 10px

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left 20px
bindsym $mod+Shift+Down move down 20px
bindsym $mod+Shift+Up move up 20px
bindsym $mod+Shift+Right move right 20px

# split in horizontal orientation
bindsym $mod+h split h
bindsym $mod+i split h

# split in vertical orientation
bindsym $mod+v split v
bindsym $mod+o split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
#bindsym $mod+s layout stacking
#bindsym $alt+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+p floating toggle
#bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child
bindsym $mod+x focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "一"
set $ws2 "二"
set $ws3 "三"
set $ws4 "四"
set $ws5 "五"
set $ws6 "六"
set $ws7 "七"
set $ws8 "八"
set $ws9 "九"
set $ws10 "十"

# sequentially workspace
workspace_auto_back_and_forth yes

# switch to workspace
bindsym $alt+Control+Right workspace next
#bindsym $alt+Shift+Up workspace next
bindsym $alt+Control+Left workspace prev
#bindsym $alt+Shift+Down workspace prev

#bindsym $alt+Shift+Left workspace $ws1
#bindsym $alt+Shift+Right workspace $ws2

bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+0 move container to workspace $ws10

# reload the configuration file
bindsym $mod+Shift+c reload
bindsym $mod+Shift+p reload

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
bindsym $mod+Shift+o restart

# exit i3 (logs you out of your X session)
#bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"
#bindsym $mod+Shift+i exec --no-startup-id "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"
bindsym $mod+Shift+i exec --no-startup-id "i3-nagbar -m 'Tidurlah wahai tuanku !!!' -B 'Tururu !!!' 'sudo shutdown -h now'"

# exit i3 and Shutdown (Shutdown Device)
#bindsym Ctrl+Shift+m exec --no-startup-id /home/mpuss/kodingan/skrip/turu.sh
bindsym Ctrl+Shift+n exec --no-startup-id /home/mpuss/kodingan/skrip/mbalen.sh


# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 20 px or 10 ppt
        bindsym Down resize grow height 20 px or 10 ppt
        bindsym Up resize shrink height 20 px or 10 ppt
        bindsym Right resize grow width 20 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
        bindsym p mode "default"
}

bindsym $mod+r mode "resize"

# class                 border  bground text    indicator child_border
#client.focused          #6A6A6A #000000 #FFFFFF #FFFFFF   #FFFFFF
#client.focused_inactive #FFFFFF #000000 #FFFFFF #484E50   #5F676A
#client.unfocused        #FFFFFF #222222 #888888 #292D2E   #222222
#client.urgent           #2F343A #900000 #FFFFFF #900000   #900000
#client.placeholder      #000000 #0C0C0C #FFFFFF #000000   #0C0C0C

# class                 border  bground text    indicator child_border
client.focused          #E00000 #E00000 #FFFFFF #E00000   #E00000
client.focused_inactive #000000 #000000 #FFFFFF #000000   #000000
client.unfocused        #e00000 #781913 #FFFFFF #000000   #000000
client.urgent           #000000 #000000 #FFFFFF #000000   #000000
client.placeholder      #000000 #000000 #FFFFFF #000000   #000000

client.background       #FFFFFF


# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
bindsym --release button3 exec --no-startup-id scrot -e 'mv $f /home/mpuss/Pictures/ss/' -q '100'
#bindsym --release button3 exec --no-startup-id i3-dmenu-desktop --dmenu="dmenu -i -fn 'JetBrainsMono ExtraBold-9' -nb '#781913' -sb '#E00000' "
#    mode hide
#    hidden_state hide
#    modifier $mod
#    separator_symbol "✿"
    position top
    bindsym button1 nop
#digae disable click button workspace
    tray_output none
    separator_symbol "〒"

#    i3bar_command i3bar --transparency
#           top right bottom left
    padding 0px 4px   0px    0px
  colors {
    background #781913
    statusline #FFFFFF
    separator  #FFFFFF

    background #450101
# class                border  bground text
    focused_workspace  #E00000 #E00000 #FFFFFF
   active_workspace   #E00000 #000000 #FFFFFF
    inactive_workspace #E00000 #000000 #FFFFFF
    urgent_workspace   #E00000 #000000 #FFFFFF
    binding_mode       #E00000 #000000 #FFFFFF
  }
        status_command i3status -c /home/mpuss/kodingan/skrip/i3status/i3statusTanggal.conf
}

#polybar statusbar config


# Make the currently focused window a scratchpad
bindsym $mod+Shift+minus move scratchpad

# Showing scratchpad window
bindsym $mod+minus scratchpad show

# Sticky floating window
bindsym $mod+Shift+plus sticky toggle

#background
#bindsym Ctrl+Shift+j exec --no-startup-id /home/mpuss/kodingan/skrip/notifbatre.sh
#exec_always --no-startup-id notify-send --icon=gtk-info Test "This is a test"
exec_always --no-startup-id feh --bg-fill /home/mpuss/Pictures/walp/shinobi-un-1366x768.jpg && /home/mpuss/kodingan/skrip/batre.sh
exec_always --no-startup-id /home/mpuss/kodingan/skrip/audio-otomatis.sh && $refresh_i3status
exec --no-startup-id xrandr --output eDP1 --primary --mode 1366x768 --pos 0x0 --rotate normal --output HDMI1 --off --output VIRTUAL1 --off
exec_always --no-startup-id dunst

#aplikasi
bindsym Ctrl+Shift+j exec --no-startup-id firefox
bindsym Ctrl+Shift+k exec --no-startup-id nemo

#lockscreen
bindsym Ctrl+Shift+l exec --no-startup-id loginctl suspend && i3lock -u -e --image /home/mpuss/Pictures/walp/png/shinobi-un-1366x768Hitambangetblur.png
##tambahano suspend cuman openrc

#screensutt
bindsym $mod+z exec --no-startup-id scrot -e 'mv $f /home/mpuss/Pictures/ss/' -q '100'
#bindsym $mod+Shift+z exec --no-startup-id shutter -s -e -o './screenshot%y-%m-%d_$w_$h.png'

#class window
#for_window [class="Gnome-terminal"]

#class workspace karo assign
exec --no-startup-id firefox
assign [class="firefox"] $ws1

#exec --no-startup-id gnome-terminal
#assign [class="Gnome-terminal"] $ws1

#exec --no-startup-id /home/mpuss/android-studio/bin/studio.sh
assign [class="jetbrains-idea-ce"] $ws2
assign [class="jetbrains-studio"] $ws2
#assign [class="kitty"] $ws2

#exec --no-startup-id nemo
#assign [class="Nemo"] $ws3

#gaps for window and gaps for workspace
gaps inner 10
workspace $ws1 gaps inner 10
workspace $ws2 gaps inner 10
workspace $ws3 gaps inner 10

#otomatis ngawang
for_window [title="terminal-ngawang"] floating enable, move left 430px, move up 192px
for_window [class="SimpleScreenRecorder"] floating enable, move left 415px, move up 55px
for_window [class="Pavucontrol"] floating enable, move right 432px, move up 172px

#autostart workspace

#color-setting
